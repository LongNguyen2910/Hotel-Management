@model Hotel_Management.Models.Bophan

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_ContentNavbarLayout.cshtml";
}

<div class="col-12">
    <div class="card">
        <div class="card-header">
            <h5 class="mb-0">Thêm thông tin nhân viên</h5>
        </div>
        <div class="card-body">
            <form asp-action="Create" method="post">
                @Html.AntiForgeryToken()

                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <div class="mb-3">
                    <label class="form-label">Mã bộ phận</label>
                    <input asp-for="Mabophan" class="form-control" type="number" />
                    <span asp-validation-for="Mabophan" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label class="form-label">Tên bộ phận</label>
                    <input asp-for="Tenbophan" class="form-control" />
                    <span asp-validation-for="Tenbophan" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label class="control-label">Ngày thành lập</label>
                    <input asp-for="Ngaythanhlap" class="form-control" />
                    <span asp-validation-for="Ngaythanhlap" class="text-danger"></span>
                </div>
                <button type="submit" class="btn btn-primary">Lưu</button>
                <a asp-action="Index" class="btn btn-secondary">Huỷ</a>
            </form>
        </div>
    </div>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

